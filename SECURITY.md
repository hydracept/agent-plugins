This repository contains no API keys, tokens, or credentials.

Cursor users set `HYDRACEPT_API_KEY` in **Plugins → Configure**. The plugin sends `Authorization: Bearer ${HYDRACEPT_API_KEY}` to https://api.hydracept.com/mcp.

Report security issues to support@hydracept.com.
